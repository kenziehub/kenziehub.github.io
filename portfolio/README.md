# Markdown Export

These files capture the current live content from the static portfolio so you can migrate it into Hugo.

## Included files

- `home.md`
- `about.md`
- `projects.md`
- `admin.md`
- `buddy.md`

## Notes

- `buddy.md` is the only non-placeholder project currently filled in.
- `Project Two` through `Project Five` are still placeholders in `data/content.js`, so they were not exported as real project files.
- If you want, the next step can be converting these into a Hugo `content/` structure directly.
